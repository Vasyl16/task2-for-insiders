import { Injectable } from '@nestjs/common';

/**
 * Authentication/authorization logic (login, refresh, token issuance) will be
 * implemented here once the feature is scheduled for development.
 */
@Injectable()
export class AuthService {}
